/**
 * @amlplugins/twilio-video
 *
 * Thin re-export of twilio (CommonJS `export =` SDK).
 *
 * Twilio Programmable Video — Rooms, Compositions, recordings, real-time video collaboration.
 */

import _sdk from "twilio";
export { _sdk as sdk };
export default _sdk;
